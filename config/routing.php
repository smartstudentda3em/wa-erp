<?php

return [

    /*
    |--------------------------------------------------------------------------
    | إعدادات توجيه المحادثات الواردة (Inbound Routing)
    |--------------------------------------------------------------------------
    */

    // الدور الذي توزَّع عليه المحادثات (فريق المبيعات).
    'sales_role' => env('ROUTING_SALES_ROLE', 'agent'),

    // السماح بالتوزيع لموظف من خارج القسم عند عدم توفّر أحد داخل القسم.
    // الافتراضي: لا — للحفاظ على فصل الأنشطة (تكييف/قرطاسية/مطبعة).
    'cross_department_fallback' => (bool) env('ROUTING_CROSS_DEPARTMENT_FALLBACK', false),

    // اعتبار الموظف غير متاح إذا لم يُشاهَد منذ (عدد دقائق).
    // null = تعطيل (الاعتماد على الحالة اليدوية available/away/offline فقط).
    'presence_timeout' => env('ROUTING_PRESENCE_TIMEOUT'),

];
